// import React from 'react'
import { Menu } from "lucide-react";
import { Button } from "../ui/button";

export default function MenuButton() {
  return (
    <Button>
        <Menu className="w-[40px]"/>
        menu
    </Button>
  )
}
